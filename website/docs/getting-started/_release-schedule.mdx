
import versionMajor from '@site/src/libs/versionMajor';
import versionMinor from '@site/src/libs/versionMinor';

{/* ------------------------------------------------------------------------ */}

## Release schedule

This distribution generally follows the official
[WineHQ](https://dl.winehq.org/wine/source/) stable release cycle,
which is one release per year, possibly with several patch releases
(like {versionMajor()}.{versionMinor()}.[0123]).
