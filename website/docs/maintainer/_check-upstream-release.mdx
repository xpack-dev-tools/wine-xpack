
import versionMajor from '@site/src/libs/versionMajor';
import versionMinor from '@site/src/libs/versionMinor';

{/* ------------------------------------------------------------------------ */}

### Check the latest upstream release

(`_check-upstream-release.mdx`)

The current release must be announced in
TODO. {versionMajor()}.{versionMinor()}

Compare the latest upstream release with the current xPack
[release](https://github.com/xpack-dev-tools/wine-xpack/releases).
If necessary, update the triplet, otherwise increase the fourth number,
as below.
